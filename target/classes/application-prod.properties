# Production Configuration Extensions
spring.jpa.hibernate.ddl-auto=validate
spring.jpa.show-sql=false
spring.jpa.properties.hibernate.format_sql=false

# In production, we assume secrets are provided via environment variables
# spring-dotenv will still work if a .env file is provided, 
# but system-level env vars are preferred for prod environments.
